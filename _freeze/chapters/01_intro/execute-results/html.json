{
  "hash": "070101c302f80b0e7b1492fe4ef98f85",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"W1. Machine Learning이란 무엇인가\"\ndescription: \"기본 용어, hypothesis space, 그리고 inductive bias\"\nauthor: \"Eunji Ha\"\ndate: today\n\n# ── AI 채팅 위젯 설정 ──────────────────────────────────────────\nformat:\n  html:\n    include-in-header:\n      text: |\n        <meta name=\"ai-title\" content=\"W1. Machine Learning이란 무엇인가\">\n        <meta name=\"ai-context\" content=\"\n        이 챕터는 machine learning의 정의와 기본 용어, hypothesis space, inductive bias를 다룹니다.\n        Machine learning은 경험(data)으로부터 규칙 f: X→Y를 찾는 귀납적 과정이며,\n        전통 통계가 '설명'을 목표로 하는 것과 달리 '예측(generalization)'을 목표로 합니다.\n        기본 용어: data set(전체 기록), instance/sample(기록 하나), attribute/feature(성질),\n        feature value, sample space, label, hypothesis(학습된 model), ground truth(실제 규칙),\n        training, testing. 학습 문제는 supervised(classification/regression),\n        unsupervised(clustering, dimension reduction), semi-supervised로 나뉩니다.\n        Hypothesis space는 알고리즘이 고려 가능한 모든 model의 집합이고,\n        version space는 그 중 training data와 모순되지 않는 가설들의 집합입니다.\n        유한한 데이터로는 version space가 하나로 좁혀지지 않기 때문에,\n        무엇을 고를지에 대한 알고리즘의 성향인 inductive bias가 반드시 필요합니다.\n        Occam's razor는 비슷하면 단순한 것을 고르는 heuristic이고,\n        No Free Lunch 정리는 모든 문제에 우월한 알고리즘은 없다는 것을 말합니다.\n        실습에서는 동심원 데이터에 로지스틱 회귀, decision tree, kNN을 적합시켜\n        결정경계를 비교함으로써 inductive bias의 차이를 시각적으로 확인합니다.\n        R은 glm, rpart, class::knn을, Python은 scikit-learn을 사용합니다.\n        \">\n---\n\n## Machine learning이란?\n\n우리가 늘 쓰던 통계 모델도 결국 데이터에서 규칙을 찾습니다.\n그럼 machine learning은 무엇이 다를까요? **질문이 다릅니다.**\n\n|                 | 전통적 통계 모델링             | Machine learning              |\n|-----------------|--------------------------------|-------------------------------|\n| 묻는 것         | 이 변수가 결과와 관련이 있는가?| 새로운 샘플의 결과를 맞히는가?|\n| 보는 숫자       | p-value, 신뢰구간, 효과크기    | generalization error, AUC     |\n| 성공의 기준     | 가진 데이터를 잘 **설명**      | 못 본 데이터를 잘 **예측**    |\n| 실패의 형태     | 가정 위반, 검정력 부족         | overfitting                   |\n\n::: {.callout-important}\n## 이 과목을 관통하는 한 문장\n\n**\"가지고 있는 데이터를 잘 맞히는 것은 아무 의미가 없다.\"**\n\n이 문장이 앞으로 15주 동안 계속 돌아옵니다.\nW3의 검증 설계, W6의 정규화, W7의 pruning, W9의 ensemble이 전부 같은 문제를 다른 각도에서 푸는 것입니다.\n:::\n\n## 기계가 \"학습\"한다는 것\n\n학습은 **경험(data) → model → 새로운 상황에 적용**의 과정입니다.\n기존 프로그래밍과 비교하면 입력과 출력이 뒤집혀 있습니다.\n\n|                  | 입력            | 출력       |\n|------------------|-----------------|------------|\n| 기존 프로그래밍  | 규칙 + 데이터   | 답         |\n| Machine learning | 데이터 + 답     | **규칙**   |\n\n즉 우리가 하려는 일은 데이터로부터 함수 $f: X \\rightarrow Y$ 를 찾는 것입니다.\n\n-   $X$ : 우리가 관측한 것 (유전자 발현량, 나이, 검사 수치)\n-   $Y$ : 우리가 알고 싶은 것 (질환군, 생존 여부, 약물 반응)\n-   $f$ : 데이터 속에 잠재된 규칙. 우리는 그 **근사치**만 얻을 수 있습니다\n\n::: {.callout-tip}\n## 왜 \"근사치만\"인가\n\n진짜 규칙(ground truth)은 아무도 모릅니다. 우리가 가진 건 그 규칙이 만들어낸 **유한한 샘플**뿐입니다.\n데이터가 100개면 100개가 허락하는 만큼만 규칙을 좁힐 수 있고, 나머지는 우리가 **가정**으로 메워야 합니다.\n그 가정이 이 장의 마지막 주제인 inductive bias입니다.\n:::\n\n## 기본 용어\n\n앞으로 계속 쓸 용어입니다. 오믹스 데이터에 대응시켜 외우면 편합니다.\n\n| 용어                    | 의미                        | RNA-seq 예시                     |\n|-------------------------|-----------------------------|----------------------------------|\n| **data set**            | 전체 경험/기록              | 환자 200명의 발현 행렬           |\n| **instance / sample**   | 기록 하나                   | 환자 1명 (또는 세포 1개)         |\n| **attribute / feature** | 대상의 성질                 | 유전자 하나                      |\n| **feature value**       | feature가 가질 수 있는 값   | 해당 유전자의 정규화 발현량      |\n| **sample space**        | feature로 만들어진 공간     | 20,000차원 공간의 점 = 환자 1명  |\n| **label**               | 우리가 맞히려는 값          | 질환군 / 생존기간                |\n| **hypothesis**          | 데이터로부터 학습한 model   | 학습된 분류기                    |\n| **ground truth**        | 실제로 잠재된 규칙          | 알 수 없음                       |\n| **training**            | data로 model을 만드는 과정  | —                                |\n| **testing**             | model로 새 샘플을 예측      | —                                |\n\n::: {.callout-warning}\n## ⚠️ 행과 열이 뒤집혀 있습니다\n\n생물정보학의 발현 행렬은 보통 **행 = 유전자, 열 = 샘플**입니다.\n그런데 거의 모든 ML 라이브러리는 **행 = 샘플, 열 = feature**를 기대합니다.\n\n전치를 빼먹으면 \"유전자 20,000개를 분류하는 모델\"이 만들어지고, 에러 없이 조용히 잘못된 결과가 나옵니다.\n**이 과목에서 가장 자주 나올 실수이니 매번 `dim()`으로 확인하세요.**\n:::\n\n::: {.panel-tabset}\n\n### R\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(2026)\n\n# 생물정보학 관행: 행 = 유전자, 열 = 샘플\nexpr <- matrix(rnorm(6 * 4), nrow = 6,\n               dimnames = list(paste0(\"GENE\", 1:6), paste0(\"P\", 1:4)))\ndim(expr)   # 6 genes x 4 patients\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 6 4\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# ML이 기대하는 모양: 행 = 샘플, 열 = feature\nX <- t(expr)\ndim(X)      # 4 samples x 6 features\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 4 6\n```\n\n\n:::\n\n```{.r .cell-code}\nround(X, 2)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n   GENE1 GENE2 GENE3 GENE4 GENE5 GENE6\nP1  0.52 -1.08  0.14 -0.08 -0.67 -2.52\nP2 -0.74 -1.02  0.11 -0.47 -0.41 -0.73\nP3 -0.22 -0.23 -2.55  1.35  0.62  0.22\nP4 -0.80  0.69 -0.33 -0.16 -1.39  1.47\n```\n\n\n:::\n:::\n\n\n### Python\n\n\n::: {.cell}\n\n```{.python .cell-code}\nimport numpy as np\nimport pandas as pd\n\nrng = np.random.default_rng(2026)\n\n# 생물정보학 관행: 행 = 유전자, 열 = 샘플\nexpr = pd.DataFrame(rng.normal(size=(6, 4)),\n                    index=[f\"GENE{i}\" for i in range(1, 7)],\n                    columns=[f\"P{i}\" for i in range(1, 5)])\nprint(expr.shape)   # (6 genes, 4 patients)\n\n# ML이 기대하는 모양: 행 = 샘플, 열 = feature\nX = expr.T\nprint(X.shape)      # (4 samples, 6 features)\n```\n:::\n\n\n:::\n\n## 학습 문제의 종류\n\nlabel이 있는지, 그리고 어떤 형태인지에 따라 나뉩니다.\n\n-   **Supervised learning** — label을 안다\n    -   **Classification** : label이 이산형 (정상/질환, 반응/무반응)\n    -   **Regression** : label이 연속형 (생존기간, 발현량, IC50)\n-   **Unsupervised learning** — label을 모른다\n    -   **Clustering** : 내재된 구조로 샘플을 나눔 (W12)\n    -   **Dimension reduction** : 구조를 저차원으로 요약 (W13)\n-   **Semi-supervised learning** — 일부만 label이 있다\n    -   소수의 환자만 조직학적 확진이 되어 있는 상황. 실제로 흔합니다\n\n한 학기 동안 우리가 지나갈 경로입니다.\n\n```\n데이터 수집\n    ↓  전처리 / Feature engineering        (W2)\n    ↓  검증 설계 + 성능 척도 결정          (W3-W4)\n    ↓  모델 학습                           (W5-W13)\n    ↓  모델 비교 및 선택                   (W4)\n    ↓  해석과 보고                         (W14)\n```\n\n## Hypothesis space와 version space\n\n### 귀납과 연역\n\n-   **Deduction(연역)** : 일반 → 특수. 공리에서 정리를 끌어내는 수학의 방식\n-   **Induction(귀납)** : 특수 → 일반. 개별 관측에서 규칙을 끌어내는 방식\n\n**Machine learning은 철저히 귀납입니다.** 그래서 태생적으로 \"틀릴 수 있음\"을 안고 갑니다.\n\n### 두 개의 공간\n\n-   **Hypothesis space** : 알고리즘이 고려할 수 있는 **모든 model의 집합**\n-   **Version space** : 그 중 **training data와 모순되지 않는** hypothesis들의 집합\n\nfeature 3개가 각각 3가지 값을 가질 수 있는 아주 작은 문제를 생각해 봅시다.\n각 feature에 대해 model은 \"특정 값이어야 한다\" 또는 \"아무 값이어도 된다(`*`)\"를 고를 수 있습니다.\n\n::: {.panel-tabset}\n\n### R\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn_values <- c(3, 3, 3)      # feature별 가질 수 있는 값의 수\n\n# feature마다 (값의 수 + '아무거나') 선택지, 마지막 +1은 공집합 가설\nprod(n_values + 1) + 1\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 65\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 유전자 20,000개를 '발현/비발현'으로 이분화하기만 해도\nn_genes <- 20000\nlog10(3) * n_genes          # hypothesis space 크기의 자릿수\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 9542.425\n```\n\n\n:::\n:::\n\n\n### Python\n\n\n::: {.cell}\n\n```{.python .cell-code}\nimport numpy as np\n\nn_values = np.array([3, 3, 3])      # feature별 가질 수 있는 값의 수\n\n# feature마다 (값의 수 + '아무거나') 선택지, 마지막 +1은 공집합 가설\nprint(np.prod(n_values + 1) + 1)\n\n# 유전자 20,000개를 '발현/비발현'으로 이분화하기만 해도\nprint(np.log10(3) * 20000)          # hypothesis space 크기의 자릿수\n```\n:::\n\n\n:::\n\nfeature 3개짜리 장난감 문제도 후보가 65개입니다.\n유전자를 발현/비발현으로만 나눠도 후보의 자릿수가 **9,500자리**를 넘습니다.\n\n여기서 결정적인 사실 하나가 나옵니다.\n\n> 유한한 training data로는 version space가 **절대 하나로 좁혀지지 않습니다.**\n\n살아남은 후보들은 training data에서는 똑같이 완벽하지만, 새로운 데이터에서는 서로 다른 답을 냅니다.\n그럼 우리는 무엇을 근거로 하나를 고를까요?\n\n## Inductive bias\n\nVersion space에 남은 후보 중 **어느 것을 선택할지에 대한 알고리즘의 성향**을 inductive bias(귀납적 편향)라고 합니다.\n\n-   어떤 알고리즘은 최대한 **일반적인** 가설을 고릅니다\n-   어떤 알고리즘은 최대한 **특수한** 가설을 고릅니다\n-   어떤 알고리즘은 경계가 **매끄러운** 것을, 어떤 알고리즘은 **축에 평행한** 것을 고릅니다\n\n::: {.callout-important}\n## 편향 없는 학습은 존재하지 않는다\n\n\"bias\"라는 단어 때문에 나쁜 것처럼 들리지만, 정반대입니다.\n**모든 학습 알고리즘은 inductive bias를 가지며, 가져야만 합니다.**\nbias가 없으면 version space에서 하나를 고를 근거가 없어 아무것도 학습할 수 없습니다.\n\n따라서 \"이 알고리즘은 가정이 없어서 객관적이다\"라는 말은 성립하지 않습니다.\n가정이 **명시적이지 않을 뿐**입니다.\n:::\n\n### Occam's razor\n\n> 비슷하게 잘 맞는 가설이 여러 개라면, 가장 단순한 것을 고른다.\n\n가장 널리 쓰이는 inductive bias입니다. 증명된 원리가 아니라 **heuristic**이라는 점이 중요합니다.\nW6의 정규화(Ridge/Lasso), W7의 pruning, W11의 prior가 모두 이 원칙을 수식으로 옮긴 것입니다.\n\n### No Free Lunch (NFL) 정리\n\n> 가능한 모든 문제에 대해 평균을 내면, 모든 학습 알고리즘의 기대 성능은 **같다**.\n\n즉 **모든 상황에서 우월한 알고리즘은 없습니다.**\n알고리즘이 잘 맞는 이유는 그 알고리즘의 bias가 **마침 그 문제의 구조와 맞았기 때문**입니다.\n\n::: {.callout-tip}\n## 연구자에게 이것이 의미하는 것\n\n-   \"어떤 알고리즘이 제일 좋나요?\"는 답이 없는 질문입니다. **\"내 데이터에서 무엇이 좋은가\"**만 답이 있습니다\n-   리뷰 논문의 벤치마크 결과를 그대로 가져다 쓰면 안 됩니다. W3에서 배울 검증 설계로 **직접 비교**해야 합니다\n-   반대로 데이터의 구조를 알고 있다면, 그 구조에 맞는 bias를 가진 알고리즘을 고르는 것이 지름길입니다\n    (샘플 수가 적고 feature가 많다 → 희소성을 선호하는 Lasso / 경계가 매끄럽지 않다 → 트리 계열)\n:::\n\n## 실습: inductive bias를 눈으로 보기\n\n같은 데이터에 세 가지 알고리즘을 적합시켜 봅니다.\n셋 다 training data는 비슷하게 맞히지만, **비어 있는 공간을 채우는 방식이 완전히 다릅니다.** 그것이 곧 bias입니다.\n\n::: {.panel-tabset}\n\n### R\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(ggplot2)\nlibrary(dplyr)\nlibrary(tidyr)\nlibrary(rpart)\nlibrary(class)\n\nset.seed(2026)\n\n# 동심원 형태의 2-class 데이터\nmake_ring <- function(n, r_min, r_max, label) {\n  theta <- runif(n, 0, 2 * pi)\n  r     <- runif(n, r_min, r_max)\n  data.frame(x1 = r * cos(theta), x2 = r * sin(theta), y = factor(label))\n}\n\ndf <- rbind(make_ring(150, 0.0, 1.0, \"A\"),\n            make_ring(150, 1.6, 2.6, \"B\"))\n\nggplot(df, aes(x1, x2, colour = y)) +\n  geom_point(alpha = 0.7) +\n  coord_equal() +\n  theme_minimal()\n```\n\n::: {.cell-output-display}\n![](01_intro_files/figure-html/unnamed-chunk-7-1.png){width=672}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 공간 전체를 촘촘한 격자로 덮고, 각 지점에서 모델의 예측을 받아옵니다\ngrid <- expand.grid(x1 = seq(-3, 3, length.out = 200),\n                    x2 = seq(-3, 3, length.out = 200))\n\n# 1) 로지스틱 회귀 — 직선 경계만 그릴 수 있음\nm_glm <- glm(y ~ x1 + x2, data = df, family = binomial)\ngrid$`Logistic regression` <- ifelse(\n  predict(m_glm, grid, type = \"response\") > 0.5, \"B\", \"A\")\n\n# 2) Decision tree — 축에 평행한 사각형 경계\nm_tree <- rpart(y ~ x1 + x2, data = df)\ngrid$`Decision tree` <- as.character(predict(m_tree, grid, type = \"class\"))\n\n# 3) kNN — 데이터를 그대로 따라가는 유연한 경계\ngrid$`kNN (k=15)` <- as.character(\n  knn(train = df[, c(\"x1\", \"x2\")],\n      test  = grid[, c(\"x1\", \"x2\")],\n      cl    = df$y, k = 15))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 주의: 그래프 라벨은 영문으로 둡니다.\n# GitHub Actions 러너에 한글 폰트가 없어 축·범례 한글이 깨질 수 있습니다.\ngrid_long <- grid |>\n  pivot_longer(-c(x1, x2), names_to = \"model\", values_to = \"pred\") |>\n  mutate(model = factor(model,\n           levels = c(\"Logistic regression\", \"Decision tree\", \"kNN (k=15)\")))\n\nggplot(grid_long, aes(x1, x2)) +\n  geom_raster(aes(fill = pred), alpha = 0.3) +\n  geom_point(data = df, aes(colour = y), size = 0.7) +\n  facet_wrap(~ model) +\n  coord_equal() +\n  theme_minimal() +\n  labs(fill = \"Predicted\", colour = \"Actual\")\n```\n\n::: {.cell-output-display}\n![](01_intro_files/figure-html/unnamed-chunk-9-1.png){width=960}\n:::\n:::\n\n\n### Python\n\n\n::: {.cell}\n\n```{.python .cell-code}\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.neighbors import KNeighborsClassifier\n\nrng = np.random.default_rng(2026)\n\n# 동심원 형태의 2-class 데이터\ndef make_ring(n, r_min, r_max, label):\n    theta = rng.uniform(0, 2 * np.pi, n)\n    r     = rng.uniform(r_min, r_max, n)\n    return np.c_[r * np.cos(theta), r * np.sin(theta)], np.full(n, label)\n\nX_a, y_a = make_ring(150, 0.0, 1.0, 0)\nX_b, y_b = make_ring(150, 1.6, 2.6, 1)\nX = np.vstack([X_a, X_b])\ny = np.concatenate([y_a, y_b])\n\nmodels = {\n    \"Logistic regression\": LogisticRegression(),\n    \"Decision tree\":       DecisionTreeClassifier(),\n    \"kNN (k=15)\":          KNeighborsClassifier(n_neighbors=15),\n}\n\n# 공간 전체를 촘촘한 격자로 덮고, 각 지점에서 모델의 예측을 받아옵니다\nxx, yy = np.meshgrid(np.linspace(-3, 3, 300),\n                     np.linspace(-3, 3, 300))\ngrid = np.c_[xx.ravel(), yy.ravel()]\n\nfig, axes = plt.subplots(1, 3, figsize=(12, 4))\nfor ax, (name, model) in zip(axes, models.items()):\n    model.fit(X, y)\n    ax.contourf(xx, yy, model.predict(grid).reshape(xx.shape), alpha=0.3)\n    ax.scatter(X[:, 0], X[:, 1], c=y, s=6)\n    ax.set_title(f\"{name}  (train acc = {model.score(X, y):.2f})\")\n    ax.set_aspect(\"equal\")\nplt.tight_layout()\nplt.show()\n```\n:::\n\n\n:::\n\n세 그림을 나란히 놓고 보세요.\n\n-   **로지스틱 회귀** : 직선 하나로는 동심원을 가를 수 없어 정확도가 0.5 수준입니다. bias가 문제 구조와 전혀 맞지 않는 경우입니다\n-   **Decision tree** : 축에 평행한 사각형을 이어 붙여 원을 흉내 냅니다. 계단 모양 경계가 트리의 bias를 그대로 보여줍니다\n-   **kNN** : 원형 경계를 거의 정확히 찾아냅니다. 다만 데이터를 너무 충실히 따라가므로 노이즈까지 따라갈 위험이 있습니다\n\n::: {.callout-note}\n## 직접 해보기\n\n1.  `make_ring`의 반지름을 바꿔 두 class가 서로 겹치게 만들어 보세요. 세 모델 중 무엇이 가장 크게 흔들립니까?\n2.  kNN의 `k`를 1, 15, 100으로 바꿔 보세요. `k = 1`의 경계는 왜 그렇게 생겼을까요?\n3.  `x1`, `x2` 대신 `x1^2 + x2^2`를 feature로 넣으면 로지스틱 회귀는 어떻게 될까요?\n    → 이것이 W10 kernel method의 핵심 아이디어입니다.\n:::\n\n## 정리\n\n| 개념                  | 한 줄 정의                                  | 이 과목에서의 위치                      |\n|-----------------------|---------------------------------------------|-----------------------------------------|\n| **Hypothesis space**  | 알고리즘이 고려 가능한 모든 model의 집합    | 알고리즘 선택 = 이 공간의 선택          |\n| **Version space**     | training data와 모순되지 않는 가설들        | 데이터만으로는 하나로 좁혀지지 않음     |\n| **Inductive bias**    | 무엇을 고를지에 대한 알고리즘의 성향        | 모든 알고리즘이 가진다. 없으면 학습 불가|\n| **Occam's razor**     | 비슷하면 단순한 쪽                          | W6 정규화, W7 pruning, W11 prior의 뿌리 |\n| **No Free Lunch**     | 모든 문제에 우월한 알고리즘은 없다          | \"내 데이터에서\" 비교해야 하는 이유 (W3) |\n\n## 다음 시간 예고\n\n-   결측치, scaling, categorical encoding\n-   Feature selection: filter / wrapper / embedded\n-   **Data leakage** — 전처리를 fold 안에서 해야 하는 이유\n-   p ≫ n 오믹스 데이터의 특수성\n",
    "supporting": [
      "01_intro_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}